import math
height=float(input("Enter the height of the cyclinder:"))
radius=float(input("Enter the radius of the cyclinder:"))
volume=math.pi*math.pow(radius,2)*height
print("Volume of the cyclinder is:",volume)

